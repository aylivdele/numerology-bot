// import type { Context } from '#root/bot/context.js'
// import { removeKeyboard } from '#root/bot/helpers/keyboard.js'
// import { Menu } from '@grammyjs/menu'

// export const startupMenu = new Menu<Context>('startup').text('Начать настройку', ctx =>
//   ctx.menu.close({ immediate: true })
//     .finally(() => ctx.conversation.enter()))
